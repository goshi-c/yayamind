# YayaMind 测试用例清单

说明：每条测试通过后，直接把 `[ ]` 改成 `[x]`。后续新增功能时，在对应模块下继续追加新的勾选项。

## 启动与基础状态

- [ ] 打开 `http://127.0.0.1:5173`，页面能正常显示三栏工作台。
- [ ] 访问 `http://127.0.0.1:8787/api/bootstrap`，返回 200。
- [ ] 访问 `http://127.0.0.1:8787/api/ai/status`，能看到 AI provider 和配置状态。

## 语音理解与纠错

- [ ] 说 `项目代办新增一个写PPT`，应先修正为 `项目待办新增一个写PPT`，再写入标题 `写PPT`。
- [ ] 说 `工作阶段新增一个写PPT`，应写入 `工作阶段` 项目，标题只保留 `写PPT`。
- [ ] 说 `明天下午有个面试`，应识别为明天，并追问具体几点。
- [ ] 被追问具体时间后，补说 `后天下午三点到三点半`，应合并上一句“面试”，新增后天下午 15:00-15:30 的面试日程，不应再追问“要干什么”。
- [ ] 被追问具体时间后，补说 `来那2:20`，应继承上一句“明天下午有个面试”的上下文，识别为明天 14:20 的面试，不应再追问“这个时间段的安排是什么”。
- [ ] 被追问具体时间后，补说 `两点二十`，应继承上一句“明天下午有个面试”的下午语义，识别为明天 14:20 的面试，不应落成凌晨 2:20。
- [ ] 小猫追问 `具体几点开始` 后，应自动重新进入听写状态，不需要再手动点小猫。
- [ ] 说 `今天晚上要去健身`，应先进入小猫理解，再因缺具体时间追问。
- [ ] 听写过程中不应提前弹出“缺具体事项/缺时间”的追问。

## 日程管理

- [ ] 说 `明天下午三点有个面试，提前准备简历和作品集`，应新增明天 15:00 的面试日程。
- [ ] 说 `明天下午三到四点有个面试`，应新增明天 15:00-16:00 的面试日程，不应识别成凌晨 3:00-4:00。
- [ ] 说 `明天下午3:00~4:00有机器人会议`，应新增明天 15:00-16:00 的会议，不应记成凌晨。
- [ ] 说 `把明天的机器人会议改到下午3:00~4:00`，应修改明天那条会议为 15:00-16:00，不应改到后天或拉长到晚上。
- [ ] 新增两个时间重叠的日程，应出现冲突选择，不直接覆盖旧日程。
- [ ] 拖动一周视图中的日程块，时间应按 10 分钟粒度保存。
- [ ] 拖动日程块上下边缘，开始或结束时间应正确改变。
- [ ] 点击未来日程视图，即使只有一天未来日程，列宽也应保持和一周视图一致。

## 项目待办

- [ ] 说 `学校新增一个待办，周四要提交开题报告的表格版`，应写入 `学校` 项目，标题为 `提交开题报告的表格版`。
- [ ] 上一条待办应显示周四日期 tag，不应把 `学校新增一个待办` 写进标题。
- [ ] 说 `学校新增一个待办，周四下午三点要提交开题报告`，应把 `时间：下午3点` 写入备注。
- [ ] 说 `今晚一定要把地拖了`，应写入待办 `拖地`，并把今晚完成要求写入备注。
- [ ] 新建空项目后按回车，应保存项目名并聚焦到第一条待办输入。
- [ ] 修改待办标题后点击旁边失焦，页面应立刻显示新标题。
- [ ] 点击待办圆圈，光标应是点击手指，待办应变浅灰、删除线并下沉。
- [ ] 长按拖动待办卡片，光标应是系统抓取手势，不应出现猫爪拖拽图。
- [ ] 把待办拖到另一个项目，项目归属应更新。
- [ ] 把待办拖到右侧月历日期，应生成月/日日期 tag，并出现对应色点。

## 右侧详情

- [ ] 点击某一天，右侧应显示该日具体安排。
- [ ] 点击一周日程中的色块，只应切换右侧到对应日期总览；再点击右侧某条安排，才进入该安排编辑状态。
- [ ] 右侧详情文字应是暖棕色层级，不应过浅或纯黑。
- [ ] 点击详情中的字段进入编辑，失焦后应自动保存。
- [ ] 只有带具体时间点的待办，才应显示到右侧截止任务中。
- [ ] 只有日期、没有具体时间点的待办，应留在项目待办里，不挤进右侧详情。

## 小猫浮窗

- [ ] 点击小猫后开始听写，停顿后自动理解/写入。
- [ ] 普通反馈消息约 10 秒后应自动消失。
- [ ] 空闲时小猫不显示气泡，头像进入睡眠/休息态；点击后再唤醒并开始听写。
- [ ] 小猫头像可拖动位置，普通点击不应误触发拖动。

## 提醒、天气与记录

- [ ] 新增外出类日程，如 `明天上午九点去医院`，满足天气条件时应出现出门提醒。
- [ ] 到点提醒应能在小猫气泡或浏览器通知中出现。
- [ ] 提醒支持完成、稍后、忽略。
- [ ] 说 `开始写论文`，应写入工作开始记录。
- [ ] 说 `写了一半`，应写入进度记录。
- [ ] 说 `今天先做到这里`，应写入结束记录。

## 目标、画像与总结

- [ ] 新增阶段性目标后，目标列表应显示新目标。
- [ ] 修改目标状态后，目标状态应更新。
- [ ] 个人画像页面应能显示当前画像信号。
- [ ] 点击生成今日总结，应在 `personal-assistant-data/summaries/` 生成 Markdown。
- [ ] 点击生成周总结，应在 `personal-assistant-data/summaries/` 生成 Markdown。

## 视觉与布局

- [ ] 全站中文字体应偏手写/铅笔感，不应明显像默认黑体。
- [ ] 分栏拖拽缩放时，文字和布局不应明显挤压重叠。
- [ ] 月历 deadline 色点应和项目色保持一致。
- [ ] 待办完成下沉动画应自然，不应闪烁或跳位。
