# PRD - YayaMind 1.1 产品需求文档

## 1. 文档信息

- 产品名称：YayaMind
- 当前版本：1.1 Schedule-only
- 更新日期：2026-06-24
- 维护者：用户 + Codex
- 版本基线：1.0 已冻结到 GitHub `main` 的 `v1.0-ai-assistant` tag。
- 关联文档：`SDD.md`、`TODO.md`、`PROJECT_CONTEXT.md`、`FILE_INDEX.md`

### 修订历史

| 日期 | 版本口径 | 修订内容 |
|---|---|---|
| 2026-06-24 | AI 化 1.0 | 冻结 1.0 基线，保留待办能力到 GitHub tag `v1.0-ai-assistant` |
| 2026-06-24 | 1.1 Schedule-only | 删除当前版本待办能力，产品主线收缩为日程安排、提醒、画像和周期日程 |

## 2. 产品定位

YayaMind 1.1 是以桌面小猫为总入口的个人日程安排助手。当前版本只保留“今天/未来要做哪些事情”的日程主线，不再提供待办列表、项目待办、待办截止日期或周期待办。

1.1 的核心体验：

```text
用户说一段安排
-> 小猫确认原文并理解
-> 有明确时间的内容生成日程
-> 缺少时间的内容进入草稿追问
-> 用户确认后写入日程 / 提醒 / 周期规则
-> 工作台刷新一周日程和今日详情
```

## 3. 版本范围

### 3.1 当前版本保留

| 编号 | 功能组 | 1.1 口径 |
|---|---|---|
| F11-01 | 一周日程和今日详情 | 保留，作为唯一主工作台 |
| F11-02 | 小猫语音输入和多轮追问 | 保留，所有安排最终落到日程或提醒 |
| F11-03 | 整组草稿 | 保留，但草稿项只允许日程、提醒和周期规则 |
| F11-04 | 日程修改 / 删除 / 批量调整 | 保留，候选范围只包含日程 |
| F11-05 | 画像与标题词表 | 保留，但不再从待办学习 |
| F11-06 | 习惯与周期安排 | 保留有明确时间的周期日程；无时间则追问 |
| F11-07 | 桌面真实入口 | 保留，1.1 收口必须同步 `D:\YayaMind` |

### 3.2 当前版本删除

- 删除导航栏“待办”入口。
- 删除项目待办页面。
- 删除今日详情里的“待办”区域。
- 删除待办月历、待办截止日期、项目分类、待办拖拽排序等当前入口。
- 删除当前版本的待办 API 入口。
- AI 不再把自然语言写成待办；无时间内容必须追问时间或作为待补充日程草稿。
- 周期规则不再生成日常待办实例，例如“每天给猫刷牙”必须追问具体时间。

旧待办源码和数据恢复方式：从 GitHub tag `v1.0-ai-assistant` 拉取 1.0。

## 4. 功能需求

### 4.1 F11-01 一周日程和今日详情

- 导航栏默认进入“一周”。
- 右侧详情只显示日程、待补充事项和提醒。
- 旧 `tasks.jsonl` 数据不展示、不参与日历、不参与画像和词表。

验收：

- 导航栏没有“待办”。
- 右侧详情没有“待办”框。
- `/api/bootstrap` 返回的 `tasks`、`todoProjects` 和日历 `day.tasks` 为空。

### 4.2 F11-02 小猫输入与日程化

- 有明确时间的输入直接生成日程。
- 没有明确时间的“任务/待办/要做某事”表达，追问“安排到几点”。
- AI 返回 `add_task` 时，后端必须转换为日程或待补充日程草稿。

验收：

- “下午三点写简历”写入 `events.jsonl`。
- “写简历”不写待办，进入追问。

### 4.3 F11-03 整组草稿

- 复杂安排仍生成整组草稿。
- 草稿项只允许日程、提醒和周期规则。
- 因缺少时间而待补充的事项留在“待补充事项”，补时间后转成日程草稿。

验收：

- “面试是三点到四点，改项目七点到八点，健身十点开始”补充后全部变成日程草稿。
- 不再出现待办草稿或待办确认按钮。

### 4.4 F11-04 习惯与周期安排

- 有明确时间的周期安排生成周期日程。
- 无明确时间的周期安排追问时间。
- 不生成周期待办。

验收：

- “每周六下午三点上课”可确认周期日程。
- “每天给猫刷牙”追问“给猫刷牙什么时间？”，不生成待办。

## 5. 非功能需求

- 本地优先：继续使用本地 JSON/JSONL 数据。
- 可恢复：1.0 待办能力通过 Git tag 恢复，不在 1.1 当前代码中保留主入口。
- 低打扰：小猫追问只问缺失时间，不解释内部状态。
- 真实入口一致：涉及桌面端的改动必须 build、desktop:pack、同步 `D:\YayaMind` 并冷启动验证。

## 6. Not Now

- 不做待办列表。
- 不做项目待办。
- 不做无时间习惯的待办化。
- 不做手机端。
- 不做全自动多日排程。
