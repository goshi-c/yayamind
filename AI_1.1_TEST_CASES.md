# AI_1.1_TEST_CASES - YayaMind Schedule-only 验收用例

## TC-01 导航不再有待办

步骤：

1. 启动应用。
2. 查看左侧导航。

预期：

- 没有“待办”入口。
- 默认可进入“一周”、习惯、画像、总结、设置。

## TC-02 今日详情无待办框

步骤：

1. 点击任意日期。
2. 查看右侧详情。

预期：

- 只看到日程、待补充事项、提醒。
- 不出现“待办”区域。

## TC-03 无时间任务表达追问时间

输入：

```text
写简历
```

预期：

- 不写入 `tasks.jsonl`。
- 小猫追问具体时间，例如“什么时候写简历？”或“安排到几点？”。

## TC-04 有时间任务表达写入日程

输入：

```text
下午三点写简历
```

预期：

- 写入 `events.jsonl`。
- 一周日程里出现对应时间块。
- `/api/bootstrap.tasks` 仍为空。

## TC-05 无时间周期习惯追问

输入：

```text
每天给猫刷牙
```

预期：

- 小猫只追问一次“给猫刷牙什么时间？”。
- 不生成周期待办。
- bootstrap 中 `tasks`、`todoProjects`、calendar day tasks 都为空。

## TC-06 有时间周期习惯生成周期日程

输入：

```text
每晚十点给猫刷牙
```

预期：

- 生成周期规则草稿。
- 确认后写入 `recurring_rules.json`。
- 日历里动态出现周期日程实例。

## TC-07 复杂草稿全部日程化

输入：

```text
今天准备面试一个小时，然后改项目一个小时，晚上健身一个小时
```

补充：

```text
面试是下午三点到四点，改项目是七点到八点，健身是晚上十点开始
```

预期：

- 三项均为日程草稿。
- 不出现待办草稿。
- 确认后写入 `events.jsonl`。

## TC-08 真实入口

步骤：

1. 执行 `npm run desktop:pack`。
2. 同步到 `D:\YayaMind`。
3. 冷启动 `D:\YayaMind\YayaMind.exe`。
4. 请求 `http://127.0.0.1:8787/api/bootstrap`。

预期：

- 返回 200。
- 真实入口 UI 与源码一致，没有待办入口。
