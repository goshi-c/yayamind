# YayaMind AI 1.0 用户验收测试用例

本文档放在项目根目录，用于用户手动验收 AI 化 1.0 新增功能。建议从真实入口 `D:\YayaMind\YayaMind.exe` 启动后测试；如果只想先看开发态，也可以在源码目录启动本地预览。

测试前建议先选一个不重要的日期或时间段，避免把真实日程弄乱。删除、批量修改、项目删除等用例执行前，先确认小猫是否展示了影响范围。

## 覆盖矩阵

| 功能组 | 覆盖用例 |
|---|---|
| AI10-F01 会话状态机和多轮上下文 | TC-01、TC-02、TC-03 |
| AI10-F02 长语音输入、原文确认和文本整理 | TC-01、TC-04 |
| AI10-F03 整组草稿与跨模块预览 | TC-01、TC-02、TC-05 |
| AI10-F04 小猫全局修改入口 | TC-06、TC-07、TC-08、TC-09 |
| AI10-F05 批量操作候选清单与确认执行 | TC-10、TC-11 |
| AI10-F06 画像与标题词表 | TC-12、TC-13、TC-14 |
| AI10-F07 习惯与周期安排 | TC-15、TC-16、TC-17、TC-18 |

## 测试用例

### TC-01 长语音整组草稿

输入： 

```text
今天下午吃饭前先准备面试一个小时，饭后改个人助手项目，晚上健身一个小时，21点50提醒我问鞋。
```

期望：

- 小猫进入整理 / 理解流程，不立即把全部内容写成正式数据。
- 生成整组草稿，至少包含日程、待办或提醒中的多类项目。
- 小猫出现确认、修改、取消类操作。
- 确认后正式写入；取消后本次草稿不保留在正式日程 / 待办 / 提醒里。

### TC-02 草稿待确认时打断

前置：先执行 TC-01，但不要确认。

输入：

```text
明天下午三点还有一个面试，地点在C楼。
```

期望：

- 新会议信息不丢。
- 旧草稿仍保持可确认 / 可取消，不被静默覆盖。
- 小猫能说明当前存在未确认草稿或等待用户处理。

### TC-03 取消当前追问或修改

前置：让小猫进入一个追问状态，例如说“把今天下午的安排往后挪一点”。

输入：

```text
算了。
```

期望：

- 只取消当前追问或修改。
- 原有正式日程不变化。
- 小猫状态回到可继续输入，不停留在理解中。

### TC-04 长语音结束后状态流转

输入：

```text
我现在说一整段，上午先整理简历，下午做项目复盘，晚上八点半健身，睡前提醒我把明天要带的材料放包里。
```

期望：

- 单击结束语音后才进入处理，不因中途停顿提前提交。
- 小猫状态依次表现为文本整理 / 理解 / 等待确认或处理完成。
- 如果需要确认，应显示草稿操作；不应一直停在“理解中”。

### TC-05 修改当前草稿

前置：先生成一个包含“晚上健身”的未确认草稿。

输入：

```text
把健身改到晚上九点。
```

期望：

- 只修改当前未确认草稿中的健身项。
- 不误改正式日程里已有的其他健身记录。
- 修改后仍可确认或取消整组草稿。

### TC-06 修改已有日程

前置：先创建一个明天下午的面试日程。

输入：

```text
把明天下午的面试地点改到C楼，备注加上提前十分钟到。
```

期望：

- 小猫能匹配到已有面试日程。
- 多候选时应展示候选，不应静默改错。
- 确认或明确匹配后，右侧详情里的地点 / 备注更新。

### TC-07 删除单条待办确认

前置：准备一条待办，例如“整理作品集素材”。

输入：

```text
删除整理作品集素材这个待办。
```

期望：

- 小猫展示将删除的待办内容。
- 删除前等待确认。
- 确认后待办消失；取消后待办保留。

### TC-08 模糊修改追问

前置：当天有下午日程。

输入：

```text
把今天下午的安排往后挪一点。
```

期望：

- 小猫不直接猜一个时间。
- 应追问“挪到几点”或展示需要补充的信息。
- 用户补充明确时间后再执行。

### TC-09 画像 / 作息直接修改

输入：

```text
我最近每天0点睡，8点起。
```

期望：

- 被识别为画像或作息修改，不写成每天睡觉日程。
- 设置或画像数据更新。
- 小猫反馈已更新作息偏好。

### TC-10 批量挪动日程

前置：今天下午至少有 2 条日程。

输入：

```text
把今天下午所有安排往后挪一小时。
```

期望：

- 右侧出现批量候选清单。
- 小猫提示等待确认。
- 确认后逐条移动，失败项要说明原因。
- 取消后所有候选不变。

### TC-11 批量项目归属修改

前置：准备几条未归类待办，其中包含项目相关内容。

输入：

```text
把今天新增的项目相关待办都归到个人助手项目里。
```

期望：

- 小猫列出将被修改的候选待办。
- 确认后这些待办的项目归属变化。
- 匹配不到的项不应静默改错。

### TC-12 标题词表归一

前置：历史里已有“健身”记录。

输入：

```text
今晚九点运动一下，一个小时。
```

期望：

- 标题优先归一为“健身”，而不是“运动一下”。
- 时间和时长正常写入。
- 右侧详情标题简洁，不把整句话塞进标题。

### TC-13 画像常见时间参与草稿

前置：历史里有稳定的健身时间，或刚测试过多条健身记录。

输入：

```text
明天安排一下健身。
```

期望：

- 如果画像有足够依据，小猫可用常见时间生成草稿，并轻量标记来自历史习惯。
- 如果依据不足，应追问时间，不凭空装懂。

### TC-14 未确认草稿不参与画像学习

操作：

1. 输入一条明显错误的草稿，例如“以后每天凌晨三点健身”。
2. 不确认，直接取消。
3. 再输入“明天安排一下健身”。

期望：

- 已取消草稿不应成为画像依据。
- 系统不应因为刚才取消的内容，把健身默认安排到凌晨三点。

### TC-15 每日长期周期规则

输入：

```text
以后每天晚上9点55提前下楼准备好我的衣服跟鞋子，健身一个小时到11点回家。
```

期望：

- 意图识别为习惯 / 周期规则，而不是 3 天普通日程。
- 小猫显示确认规则 / 修改 / 取消。
- 标题为“健身”。
- 日程时间为 22:00-23:00，9:55 作为提前准备信息，不作为健身开始时间。
- 具体安排保留用户原话语义。
- 备注包含“带上衣服和鞋子”。
- 确认后写入周期规则；周视图能动态看到可见日期实例。

### TC-16 每周固定周期规则

输入：

```text
以后每周六下午三点上课，上两个小时。
```

期望：

- 生成每周六周期规则。
- 确认后可在周六 15:00-17:00 看到动态日程实例。
- 不应只生成当前周的一条普通日程。

### TC-17 无明确时间的习惯

输入：

```text
以后每天记得复盘当天做了什么。
```

期望：

- 识别为习惯或周期提醒，而不是强行塞进某个随机时间段。
- 如果缺少提醒时间，应追问或生成无具体时间的待办 / 提醒草稿。
- 确认后不应污染正式日程时间轴。

### TC-18 修改或删除周期规则

前置：先确认 TC-15 或 TC-16 的周期规则。

输入：

```text
把每天健身这个规则改成晚上十点半开始。
```

期望：

- 小猫匹配到周期规则，而不是只改某一天实例。
- 多候选时展示候选。
- 确认后后续可见日期的动态实例时间变化。

再输入：

```text
取消每天健身这个长期规则。
```

期望：

- 小猫展示将取消的周期规则并等待确认。
- 确认后后续可见日期不再动态生成该规则实例。

## 通过标准

- 每个需要确认的用例都必须有确认 / 修改 / 取消或等价操作，不能只显示“理解中”。
- parse 或草稿阶段不得提前写入正式业务数据。
- 删除、项目删除、批量操作、周期规则修改必须展示影响范围。
- 小猫反馈、右侧详情、周视图和待办区必须在确认后同步刷新。
- 真实入口测试时，以 `D:\YayaMind\YayaMind.exe` 的表现为准。
