# Decisions

## 2026-06-04 最新补充决策

### 语音理解预览与 AI 标识

- 输入理解预览固定收敛为 `标题 / 具体内容 / 提前准备` 三块；标题是语义总结，不是原话前几个字。
- 标题优先使用用户容易扫读的类别词，例如 `开会`、`上课`、`面试`、`提醒`、`任务`；具体时间、地点、内容、备注和准备事项放到下方详情。
- AI 参与状态不写进主标题；如果 DeepSeek 参与解析，只在旁边显示小字 `DeepSeek 参与`。
- `DeepSeek 参与` 表示 AI 参与了自然语言理解候选结果，不代表 AI 已经替用户最终决策或直接改动安排。

### 新增、修改与补充意图

- 小猫听到语音后，需要先判断用户是在新增安排、修改已有安排，还是补充已有安排的信息。
- “补充/备注/准备/带上”这类表达优先匹配已有会议、面试或课程，并补充备注或准备事项。
- “修改/重新安排/不是/改到”这类表达优先尝试修改已有安排，不直接当作新日程写入。
- 若意图仍不清楚，继续只追问一个关键问题，不给用户过多选择。

### 重叠安排确认

- 时间重叠不默认禁止，日程块允许以半透明/并列方式重叠展示。
- 重叠时只提供用户动作：`修改 / 重叠 / 取消`。
- 用户选择 `重叠` 后，新旧安排都保留；用户选择 `修改` 后回到连续语音/编辑状态；用户选择 `取消` 后不处理本次输入。
- 不向用户展示“保留已有安排、记为待定、使用新时间、移动已有安排”等内部实现逻辑。
- 日程不按标题相似判断重复；多个安排都归纳为“开会”不代表重复。只有真实时间范围重叠才触发日程冲突。

### 语音写入确认

- 明确时间的日程/任务可以先写入页面，但写入后必须给用户 `确认 / 修改 / 取消`，用于处理语音识别把日期、时间、事项听错的情况。
- 写入成功后页面必须自动跳到对应日期和时间点，不能让用户自己拖动时间轴去找新加的安排。
- `确认` 表示保留当前写入；`取消` 软删除刚写入记录；`修改` 进入连续语音修正状态。
- 修改状态下，用户不需要再次点击小猫，可以直接说“不是周五，是周六”“加备注：带电脑”等补充。

### 小猫气泡

- 小猫气泡首屏默认不显示任何历史消息或占位引导，避免刚打开网页就出现“已删除这条……”之类不属于当前上下文的话。
- 气泡只在听写、思考、追问、确认、修改、输入预览等有真实交互内容时出现。
- 点击小猫开始说话，按住并移动才拖动头像；点击和拖动必须分开判定，避免头像黏在鼠标上。

### 一周安排与右侧详情

- 中间一周安排是全周缩略视图，尽量保持周一到周日都可见；详细内容放在右侧日期详情里。
- 中间日程块只承担缩略预览和时间定位，不展开完整详情；块内只显示短标题，不写几点到几点。
- 日程时间范围通过背景时间轴、块的位置和块高度表达；右侧详情再展示具体几点到几点。
- 右侧日期详情是主要编辑区，具体安排、待补充事项、截止任务、提醒都在这里展开和编辑。
- 右侧内联编辑保持克制：标题、月/日、开始/结束时间、准备事项和操作按钮；不展示“目的”和“备注”这类容易重复的字段。
- 右侧可以独立滚动；左侧导航和中间一周安排不跟随整页滚动。

### 视觉与时间轴

- 当前视觉方向继续使用浅色、暖色、纸感，不使用冷蓝色会议卡片。
- 今天列使用半透明背景，应该融入底层背景，而不是整块不透明覆盖。
- 时间轴保留内部滚动，不再拉得过长；当前实现高度为 `1900px`，用于兼顾可读刻度和“长方形”整体观感。
- 日程块使用透明玻璃描边效果，而不是普通实心色块或磨砂遮罩；背景小时数字和虚线必须能从日程块后面透出来。
- 标题在日程块内水平和垂直居中，避免文字贴边和边框混在一起。

## 已确认决策

### 产品定位

- 项目是一个生活化、陪伴感强的 AI 个人助手，不是普通 TODO 或日历工具。
- 第一阶段先做给自己用，后续再包装成作品集项目。
- 核心价值是：计划安排、真实执行记录、动态重排、复盘原因沉淀和个人画像。

### 第一版形态

- 第一版先做 Web 模拟版，不直接做真正桌面应用。
- 网页中模拟小猫浮窗，后续再迁移到 Electron 或 Tauri 之类的桌面壳。
- 暂不做手机 App、微信入口或小程序。

### 入口体验

- 助手形象优先使用可爱小猫。
- 小猫要有互动感，例如点击、拖动、气泡反馈，后续可做靠边隐藏、探头、挠屏幕等。
- 日常语气偏可爱、陪伴、有互动感。
- 复盘和计划偏差分析时切换为严谨的复盘助手风格。

### 输入方式

- 第一版支持语音输入和文字输入；V1.1 后小猫浮窗优先使用语音对话。
- 语音输入实时识别成文本，让用户确认后再提交。
- 小猫区域只保留一个对话气泡：反馈、追问、选择和理解预览都收敛在这个气泡内，避免多个追问框重复出现。
- 普通底部文本输入框不作为主入口保留；后续如加文字入口，也应是辅助能力，不破坏“小猫对话优先”的体验。
- 普通无冲突事项默认直接记录，不额外确认。
- 有重复、冲突、重排、时间不明确时再向用户确认。
- 新增、修改、补充必须先做意图识别：准备/带上优先补充已有安排；修改/不是/改到优先修改已有安排；其余才按新增走冲突判断。

### Web 面板布局

- 左侧放计划、阶段性目标、人物画像、复盘、设置等长期维度。
- 中间是自然周一周安排，类似周视图；首页第一屏继续保持一周安排。
- 右侧是选中日期详情，而不是固定今日面板。
- 点击某一天时，右侧按时间顺序列出当天具体安排和执行记录，默认只展示必要信息。
- 点击具体小事件块时，右侧打开内联编辑，可修改标题、月/日、开始/结束时间和准备事项，也可删除。
- 日历视图需要当前时间线、过去时间弱化、类型颜色区分。
- 时间轴覆盖 0:00-24:00，兼容凌晨安排和较晚作息。
- 时间块可拖动移动，也可拖上下边缘调整开始/结束时间，按 10 分钟粒度吸附。
- 时间冲突只作为轻量小标签显示，不在右侧详情里重复解释，也不默认引导用户调整。

### MVP 功能范围

- 支持开始、暂停、结束工作。
- 支持添加日程。
- 支持添加任务。
- 支持安排时间块。
- 支持记录进度。
- 支持动态重排。
- 支持记录没完成原因。
- 支持生活事项和提醒。
- 支持阶段性目标。
- 支持个人画像雏形。
- 支持浏览器内提醒。

### 数据存储

- Obsidian 作为本地数据仓库。
- 核心数据优先使用程序易读的 JSON/JSONL。
- 网页端负责将数据渲染成人能读的日历、面板和总结。
- 总结类内容可以生成 Markdown 放回 Obsidian。

### AI 策略

- 不让所有功能都依赖强模型。
- 时间解析尽量规则优先，AI 兜底。
- 类型判断使用 AI。
- 当前 DeepSeek adapter 已接入：有本地 key 时优先 AI 解析，无 key 或失败时回退本地规则。
- 即使 AI 给了猜测时间，只要原文是模糊时间且没有具体几点，也必须追问；明确时间段不应误追问。
- 冲突判断用程序逻辑。
- 日程冲突判断只统计 `scheduled` 日程的真实时间重叠；已取消/已移动事项不参与时间冲突；日程不按标题相似判断重复。
- 重排建议用简单规则加 AI 表达。
- 深度周报、长期画像总结低频使用更强模型。

### 日期详情和提醒

- 右侧日期详情默认保留“具体安排、出门提醒、执行记录”三类信息，避免普通提醒列表挤占空间。
- 普通提醒能力仍保留在数据层和通知链路里，但不作为右侧详情的主要展示内容。
- 天气/出门提醒使用轻量策略：后端读取 7 天游雨概率，识别外出类安排，在出门前给“带伞”等准备提示。
- 默认天气城市先使用设置里的上海坐标；后续可做城市配置或浏览器定位。

### 视觉方向

- V1.1 视觉方向从深色工作台转向浅色、橘黄/肉色、白色为主的涂鸦可爱风。
- 视觉要可爱但仍适合日常使用；冲突提示不用强烈大红，优先用暖色边线和状态文字。

### 暂不做

- 自动看屏幕或截图分析。
- 复杂自动排程算法。
- 系统级常驻提醒。
- 多用户账号系统。
- 睡眠/状态记录。
- 特别精致的小猫动画。

## 2026-06-07 项目待办交互决策

- 项目待办是独立于一周日程的导航功能，默认按项目分类，不按日期分组。
- 项目分类可以增删改，但新增项目不使用单独表单；点击 `新增项目` 后直接生成一个可编辑的项目标题。
- 项目名称、待办标题、备注都采用点击文字原地编辑；失焦自动保存，不再展示显眼的保存按钮。
- 待办完成后不从列表消失，而是下沉到所属项目底部，并用浅灰色和删除线表示完成。
- 日期 tag 由任务 `dueAt` 派生，和备注分离展示；tag 不可直接编辑，但可通过清除 deadline 或重新拖到日历日期来修改。
- 待办拖到月历日期用于设置 deadline；拖到另一个项目分组用于移动项目归属。
- 一周日程中间视图不再展示截止任务线，避免和项目待办重复；右侧日期详情仍需要展示当天截止任务。
- 明确说 `新增/新加/添加/有一个/待办/项目代办` 的语音或文本，应优先进入新增任务意图，不应先弹出“是否新增安排或任务”的二次确认。

## 2026-06-07 语音纠错、待办截止和视觉手感决策

- 语音输入的顺序固定为：实时显示浏览器转写，停顿后先做转写纠错，再进入意图理解；只有解析后仍缺信息时才追问。
- `今天/明天/后天/周几` 这类日期必须由本地规则兜底校正，避免 AI 把相对日期推到错误日期。
- `项目名 + 新增待办 + 内容` 的句式应把项目名前缀剥离，只把真正要做的事写入待办标题。
- 待办日期 tag 只展示月/日，不展示年份；用户说到具体时间点时，时间点写入备注。
- 右侧日期详情只展示带具体时间点的截止任务；只有日期的普通待办仍留在项目待办模块和月历色点中。
- 项目待办完成圆圈是点击行为，使用点击指针；卡片拖拽使用系统抓取手势，不再使用自定义猫爪拖拽图。
- 小猫反馈消息不长期停留，约 10 秒后回到默认倾听提示。
- 视觉字体优先尝试楷体、仿宋和文楷类字体；右侧详情文字使用暖棕深浅层级，避免纯黑或过浅灰。
