# TODO

## 当前版本

- 版本号：MVP 收口版
- 当前阶段：MVP 功能收口完成；保留真实桌面语音人工验收项，准备进入 AI 化 1.0
- 本轮目标：完成 MVP 最终收口审计，确认 `PRD.md` / `SDD.md` 与当前实现匹配，更新归档摘要，并把 1.0 开发入口提上日程
- PRD 对应章节：`PRD.md` 第 4、5、9、10 节
- SDD 对应链路：`SDD.md` 第 6、7、9、10 节

## 文档读取规则

- 日常开发主读：`PRD.md`、`SDD.md`、`TODO.md`。
- 文件定位：进入代码定位、日志定位、数据结构定位或模块定位阶段时再查 `FILE_INDEX.md`。
- 索引维护：如果本轮找到了 `FILE_INDEX.md` 没覆盖的文件、目录、日志或数据位置，收尾时补入 `FILE_INDEX.md`。
- 历史回溯：`PROJECT_CONTEXT.md`，按编号、日期或关键词定位。
- 模块深挖：`docs/sdd/`，只在改对应模块时读取。
- 外部展示：`README.md`、`docs/interview/`、`docs/portfolio/`，不作为日常开发依据。
- 历史遗留：`docs/archive/TASK_LOG.md`、`docs/archive/DECISIONS.md`，只查旧验证证据或旧决策原因，不再默认写入。

## MVP 最终收口结论

- [x] 对照代码入口确认 MVP 核心功能均有实现链路：工作台、一周安排、自然语言输入、右侧详情、项目待办、本地数据层、桌面小猫、设置页、节假日 / 调休。
- [x] 对照 `PRD.md` / `SDD.md` 确认当前功能范围、页面状态、异常兜底、数据目录、真实入口边界和 1.0 隔离口径匹配。
- [x] 已将 MVP 收口结论同步到 `PRD.md`、`SDD.md`、`TODO.md` 和 `PROJECT_CONTEXT.md`。
- [x] 本轮已执行 `npm run build`、`npm run desktop:pack`，并同步 `D:\YayaMind`；真实入口资源包含最新 `index-CUBEXDNA.js` / `index-BzO6TYCV.css`。
- [x] 已从真实入口 `D:\YayaMind\YayaMind.exe` 启动，`http://127.0.0.1:8787/api/bootstrap` 返回 200。

## P0 保留人工验收项

- [ ] 用户在真实桌面交互环境点击小猫说一句，确认日志出现 `commit-success`，且日程格子刷新。
- [ ] 用户肉眼确认桌面小猫悬浮窗可见、能保持在合理前景层级，单击 / 双击 / 拖动语义符合预期。

## 1.0 前置优化

- [ ] 设计新用户教程：单击开始说话、再次单击停止并提交、双击打开工作台、追问时怎么补充、听不清时怎么重说。
- [ ] 补一张当前真实入口的最终工作台 / 小猫截图，用于 `docs/portfolio/` 或后续 1.0 前展示材料。

## 1.0 工程化优化

- [ ] 把打包、同步、清理旧资源、启动真实入口验收做成一个受控的 `desktop:release-local` 脚本。
- [x] 接入 2026 年中国法定节假日/调休数据，区分休息日和补班日。

## 2026-06-19 桌面小猫悬浮窗不可见修复

- [x] 排查真实入口：主界面可打开，`YayaMind Bubble` 进程存在，`desktop-cat.log` 显示 bubble 已创建；猫图资源存在，问题不是资源缺失。
- [x] 将 `D:\YayaMindData\userData\desktop-settings.json` 的小猫位置重置到主屏可见区域 `x=1220,y=560`。
- [x] 修复 `electron/main.cjs`：小猫窗口创建后在 `ready-to-show` 和创建后兜底定时主动 `liftBubbleWindow`；二次启动和 app 激活时也走 `liftBubbleWindow`，不只调用 `show()`。
- [x] 验证：`node --check electron/main.cjs`、`npm run build`、`npm run desktop:pack` 均通过；已同步到 `D:\YayaMind`，正常可视方式从 `D:\YayaMind\YayaMind.exe` 启动，`/api/bootstrap` 返回 200；日志出现 `bubble-lift`，位置为 `x=1220,y=560,width=360,height=280`。

## 2026-06-18 右键虚线范围、右侧详情无线滚动和编辑态修补

- [x] 项目待办右键菜单撤掉“删除待办：...” / “删除项目：...”范围文字，只保留“删除”按钮；右键待办时待办卡片出现虚线框，右键项目时项目卡片出现虚线框，范围与拖拽选中范围一致。
- [x] 右侧一日详情右键改为先弹出只含“删除”的确认菜单，不再右键立即删除；被右键的日程 / 待办行用虚线框标明删除范围。
- [x] 右侧一日详情编辑态移除显式保存按钮，保留点击外部自动保存；编辑框去掉白框套橙框的双框感，标签和输入文字改为更深色。
- [x] 一周安排滚动区域、右侧日程 / 待办滚动区和项目待办列表隐藏可拖动滚动条，但保留滚轮 / 触控板滚动。
- [x] 一周右侧详情移除“今天 / 日程 / 待办”之间的所有横向分割线；待办区略微下移，内容多时在透明滚动区内上下滚动。
- [x] 休息日周视图背景和休息日日程块改为偏深蓝色系；时间轴休息块改为更深灰色，并加密集斜杠纹理表达休息区间。
- [x] 验证：`npm run build` 通过；Browser 开发态验证待办右键和项目右键菜单均只显示“删除”且虚线框范围正确；右侧详情右键只显示“删除”且行数未变化；右侧详情无线、滚动条 `scrollbar-width: none` 且 `overflow-y: auto`；休息日蓝系背景、休息块斜杠、双击编辑无保存按钮且字体为深色。
- [x] 正式入口同步：`npm run desktop:pack` 通过；已同步 `D:\YayaMindBuild\release\win-unpacked` 到 `D:\YayaMind`；停止开发端口后从 `D:\YayaMind\YayaMind.exe` 冷启动，`/api/bootstrap` 返回 200；真实资源包含 `todo-context-selected`、`detail-context-selected`、`detail-context-menu`、深灰斜杠休息块和隐藏滚动条规则。

## Not Now

- [ ] 语音唤醒默认关闭，后续单独评估唤醒词、隐私和误触。
- [ ] 手机端另起阶段评估，优先 PWA/Capacitor，等桌面入口稳定后再做。

## 下一阶段：AI 化 1.0

- AI 化 1.0 候选版本文档：`docs/roadmap/YayaMind-AI-1.0-PRD-SDD.md`。
- 该文档用于 MVP 收口后启动 1.0 开发；当前不把 `plan_day`、今日规划草稿、画像排程、历史实体词表辅助 query rewrite、模糊时间解释器写入 MVP 必做项。
- 下一轮进入 1.0 时，先读取该 roadmap，确认 1.0 P0 范围，再把根目录 `PRD.md` / `SDD.md` / `TODO.md` 切换为 AI 化 1.0 开发版。

## 2026-06-18 右键范围、双击编辑、桌面小猫处理中状态和面试日程修补

- [x] 项目待办右键菜单曾补充删除范围说明；后续已按用户反馈改为只显示“删除”，范围由虚线框表达。
- [x] 项目待办标题和项目标题改为双击才进入编辑；右侧一日详情单击事项只选中 / 查看，双击事项才进入内联编辑。
- [x] 右侧详情和待办编辑框按换行与长句折行动态计算行数，不再硬限制两行；编辑态和查看态字号 / 行高保持一致。
- [x] 一周日程有事色块继续加深但保持透明；一周详情分割线方案后续已按用户反馈改为无线布局，项目待办页右侧月历内栏不出现多余分隔线。
- [x] 项目和待办普通悬停统一为箭头光标；长按待办 / 项目进入拖拽武装态后才显示抓取光标；双击进入输入框后才显示文本插入光标。
- [x] 桌面小猫新增 `thinking` 状态：听写中保留光圈，安排事项中改为静态站立 / 坐着状态；处理中气泡拆为上方原话、下方灰色小字状态，且不自动消失。
- [x] 桌面小猫气泡窗口高度从 190 提高到 280，消息区上限提高到 188px 并保留滚动，长语音文本会逐渐撑开但不超过窗口。
- [x] 桌面小猫打开主窗口后重新 lift 悬浮窗，减少被 YayaMind 主界面盖到后面的情况。
- [x] 修复“明天下午两点有一个面试，半个小时，准备答辩稿”被误判成补充已有日程的问题；明确新增日程优先走 `add_event`，并支持“半个小时 / 半小时”推导 30 分钟结束时间。
- [x] 验证：`npm run build` 通过；`node --check electron/main.cjs` / `electron/preload.cjs` 通过；`electron/bubble.html` 内联脚本语法检查通过；临时 `YAYAMIND_DATA_DIR` 回放该面试语音，返回 `intent: add_event`，写入 `events.jsonl`，时间为 2026-06-19 14:00-14:30，准备事项保留“我的答辩稿”。
- [x] Browser 开发态验证：上一轮曾验证一周详情两条分割线；后续已按用户反馈改为无线布局。保留本条作为历史验证记录，不作为当前验收口径。
- [x] 正式入口同步：`npm run desktop:pack` 通过；已同步 `D:\YayaMindBuild\release\win-unpacked` 到 `D:\YayaMind`；关闭开发端口后从 `D:\YayaMind\YayaMind.exe` 冷启动，`/api/bootstrap` 返回 200；真实资源包含 `state-thinking`、`open-main-after-front`、`安排事项中` 处理中拆栏逻辑和本轮右键 / 双击代码。
- [ ] 仍需真实入口人工确认：真实桌面上小猫是否始终浮在主界面前；真实说一句面试日程后是否出现 `commit-success` 并刷新日程格子；处理中小猫是否静态且气泡不提前消失。

## 2026-06-18 语音待办完整写入、时间轴和取消对话修补

- [x] 语音纠错收紧为轻纠错：不总结、不改写成短标题、不删除任务细节；安全纠错长度下限提高到原文 75%。
- [x] AI 意图识别增加已有项目词表；项目待办识别到近似项目名时优先回填已有项目，不新造项目名。
- [x] 规则层增加项目 alias 兜底：`外coding` 可匹配语音转写里的 `web coding / webcoding`。
- [x] 待办落库增加标题守卫：AI 返回“同步 / 任务 / 处理”等过短泛化标题时，回退到原话中“待办”后的完整事项内容。
- [x] 手动新增待办支持非空标题失焦自动保存；备注为空也会退出编辑态，空草稿失焦仍自动收起。
- [x] 一周时间轴固定 0:00-24:00；作息时间只生成灰色“休息”块，不再裁剪一天范围。
- [x] Web 小猫对话框更贴近小猫脚边，并新增右侧取消按钮；取消会舍弃本次输入、追问、确认和写入后修改状态。
- [x] 桌面悬浮气泡新增取消按钮，通过 IPC 通知主窗口舍弃当前语音会话，并把小猫切回休息。
- [x] 验证：`npm run build` 通过；`node --check electron/main.cjs` / `electron/preload.cjs` 通过；`electron/bubble.html` 内联脚本语法检查通过；Browser 开发态验证 0:00-24:00 时间轴和“休息”块、非空待办失焦自动保存；临时数据目录回放 `web coding新加一条待办让他把桌面的入口同步一下` 成功写入完整标题并归入已有 `外coding` 项目。
- [x] 正式入口同步：`npm run desktop:pack` 通过；已同步 `D:\YayaMindBuild\release\win-unpacked` 到 `D:\YayaMind`；从 `D:\YayaMind\YayaMind.exe` 冷启动后 `/api/bootstrap` 返回 200；真实资源包含 `cancel-voice` 和 `sleep-block`。
- [x] 追加修补：时间轴里的睡眠时段压缩为一个 1 小时高度的灰色“休息”块，内部只保留半小时分割线，不再展示休息区间内连续小时刻度。
- [x] 追加修补：右侧一日详情的“日程 / 待办”之间增加更明确的分割线，降低两区混在一起的感觉。
- [x] 追加修补：AI 意图识别 prompt 要求日程 title 由用户原话归纳为 2-3 个中文短标题，不机械截取前三个字；前端旧数据展示兜底修正“重构我”等异常短标题。
- [x] 追加验证和同步：`npm run build`、`npm run desktop:pack` 通过；已再次同步到 `D:\YayaMind`，从 `D:\YayaMind\YayaMind.exe` 冷启动后 `/api/bootstrap` 返回 200；真实资源包含 `sleep-block`、休息文本、半小时虚线、`改skill`、`重构我` 和 `today-task-zone`。
- [ ] 仍需真实入口人工确认：桌面气泡叉按钮能关闭当前系统听写面板并舍弃文本；真实长语音不再只写两三个字；0:00-24:00 时间轴和灰色“休息”块视觉密度合适。

## 2026-06-17 一日详情、待办删除和设置页修补

- [x] 右侧一日详情标题中，今天只显示“今天”，不再显示周几和日期。
- [x] 右侧详情中待办只显示截止时间和待办内容，不再显示项目分类、“具体安排”或“备注”等标签。
- [x] 待办编辑框按内容动态计算行数，编辑态字号和行高与预览态保持一致。
- [x] 去掉项目待办和右侧详情里的可见删除按钮；右键待办删除该待办，右键项目空白区域删除项目并把任务迁移到未归类。
- [x] 未归类改为兜底收纳区；语音/文本识别到不存在的项目名时，不自动新建项目，任务落入未归类。
- [x] 桌面小猫在 parse / commit / refresh 过程中保持站立和处理中反馈，气泡显示用户原文和灰色“我听到了，正在理解中”提示。
- [x] 桌面小猫透明悬浮窗改为鼠标穿透：只有小猫本体区域可点击/拖动，周围透明区不再挡住后方文档或网页操作。
- [x] 左侧底部新增设置入口，支持保存 AI provider、base URL、model、API Key；后端 AI Adapter 优先读取本地设置，未配置时回退环境变量。
- [ ] 仍需真实入口人工确认：右键删除项目/待办、语音写入时小猫不会提前睡觉、设置页保存后 AI 调用走本地配置、小猫透明区可点透到底层应用。

## 2026-06-17 右侧详情密度、小猫阶段状态和未归类兜底修补

- [x] 右侧一日详情用分隔线拆成“今天标题 / 日程 / 待办”三块；日程区和待办区均为独立滚动区。
- [x] 右侧待办查看态取消单行省略，改为更小字号和完整换行显示；编辑态字号、行高同步压小，避免内容看不清。
- [x] 项目待办右键不再直接删除，改为鼠标旁“删除 / 不删除”小菜单，只有确认删除才调用删除接口。
- [x] 前后端都保护未归类：未归类不能删除；历史同名“未归类”项目按兜底区处理；未匹配项目名的语音 / 文本待办落入唯一未归类。
- [x] 小猫语音反馈拆成原话 / 转写和灰色阶段状态，阶段文案为“文字整理中 / 理解意图中 / 安排事项中 / 完成了”，完成后延迟消失。
- [x] 验证：`npm run build` 通过；Browser source/dev 验证普通待办右键弹出“删除 / 不删除”，右键未归类不弹删除菜单并提示不可删除；右侧详情标题为“今天”，日程 / 待办区独立滚动，查看态文字 `12.5px` 且完整换行。
- [ ] 仍需阶段收口时打包 / 同步真实入口后，用户在 `D:\YayaMind\YayaMind.exe` 人工确认语音状态文案、右键菜单和右侧详情视觉效果。

## 2026-06-17 待办拖拽旧能力回归修复

- [x] 恢复 deadline 日期 tag 的颜色样式，避免右侧操作按钮通用样式覆盖 `todo-date-tag`。
- [x] 恢复待办拖到右侧月历日期后设置 deadline 的链路：月历日期补 `data-todo-date`，拖动经过时显示蓝色日期落点，松手后走 deadline 更新逻辑。
- [x] 补齐 SDD 项目待办链路，明确待办排序、跨项目移动、拖到月历改日期、日期 tag 颜色、月历色点和右侧日期详情需要一起回归。
- [x] 复盘沉淀到 `productize/PRODUCT_LESSONS.md` 经验 022，并把“功能链路回归闸门”写入 `productize/SKILL.md`。
- [x] 验证：`npm run build` 通过；Browser 开发态验证 `#6/16` 日期 tag 恢复有色背景 / 边框，月历日期带 `data-todo-date`；`npm run desktop:pack` 通过；已同步真实入口 `D:\YayaMind`，冷启动后 `/api/bootstrap` 返回 200，真实资源包含本轮日期落点和 tag 样式。
- [ ] 仍需用户在真实入口人工长按待办拖到右侧月历某天，确认松手后日期 tag、月历色点和右侧日期详情同步更新。

## 2026-06-16 云转文字链路收敛
- [x] 固定本轮范围：只处理云转文字和小猫听写闪退，不动后续 AI 转写/理解策略。
- [x] 回溯当前失败原因：未闭环的 Electron 录音后端 STT 分支接入主入口，但 preload/前端没有对应录音接收链路。
- [x] 放弃继续修 WinRT helper 小分支：smoke test 证明 PowerShell 5 加载 WinMD 不稳定，且安装版日志已出现 0.5 秒 `No speech result`。
- [x] 桌面小猫听写回退到 Windows 系统听写桥：点击触发 `Win + H`，隐藏输入框接收系统听写文字，第二次点击停止并提交。
- [x] 第二次点击停止时不再触发新的 `Win + H`；停止后回休息态并保留理解结果气泡。
- [x] 补齐真实入口日志链：`native-voice-stop`、`desktop-recognized-text`、`parse-preview-start/success/error`、`bubble-message`、`bubble-lift`。
- [x] 小猫气泡不再显示内部意图标签“我理解成：日程 / 时间块”，改为缺信息追问或自然语言整理结果。
- [x] 桌面语音解析成功且无需确认时自动写入日程；需要确认/冲突时保留追问，不自动误写。
- [x] 补齐提交阶段日志：`desktop-auto-commit-start`、`commit-start`、`commit-success`、`commit-needs-confirmation`、`commit-failed`。
- [x] 已通过 `node --check electron/main.cjs`、`node --check electron/preload.cjs`、`npm run build`、`npm run desktop:pack`。
- [x] 已同步到真实入口 `D:\YayaMind`，并从 `D:\YayaMind\YayaMind.exe` 启动验证 `/api/bootstrap` 返回 200。
- [ ] 需要用户在桌面交互环境点击小猫说一句，确认日志出现 `commit-success`，且日程格子刷新。

## 2026-06-15 桌面小猫语音提交与 rewrite 展示修复
- [x] 发现 Electron Web Speech 在当前网络环境会报 `network`，不能作为桌面版主识别源。
- [x] 桌面小猫听写改为“双通道”：Windows `System.Speech` 作为主识别源，通过 `desktop-cat:voice-partial` 实时回传；Electron Web Speech 仅作为兜底尝试。
- [x] Electron `media/microphone` 权限判断改为信任主窗口/小猫窗口，并记录 `pageUrl`、`requestOrigin`，避免空 origin 被误拒。
- [x] 前端补充语音诊断日志：Web Speech 不可用、ready、start、result、error 都会写入 `desktop-cat.log`。
- [x] 重新打包并同步到真实入口 `D:\YayaMind`；从 `D:\YayaMind\YayaMind.exe` 启动后 `/api/bootstrap` 返回 200。
- [ ] 仍需用户用真实麦克风点击小猫说一句，确认日志出现 `windows-speech-partial`，并在气泡看到实时转写。
- [x] 第二次点击小猫改为“说完并提交”，不再当作取消当前会话。
- [x] 语音识别 partial 改为累计文本实时回传，避免气泡只显示最后一小段。
- [x] 结束听写后先调用 `/api/input/parse` 做意图识别和 query rewrite，并在理解卡片展示“实时转写 / AI改写”。
- [x] 理解卡片补充“确认记录”按钮，rewrite 和 intent 可见后再写入。
- [x] 修复并验证 Windows PowerShell 语音脚本语法，避免再次出现脚本解析错误导致听写启动失败。
- [x] 重新执行 `npm run desktop:pack`，并同步到真实入口 `D:\YayaMind`。
- [x] 从 `D:\YayaMind\YayaMind.exe` 启动验证，本地 API `/api/bootstrap` 返回 200。
- [ ] 仍需用户用真实麦克风说一句完整内容，确认 Windows 语音引擎在当前设备上能产出 partial/final 文本。

## 2026-06-15 桌面语音与小猫气泡修复

- [x] 修复 Windows 桌面语音识别进程在空结果/超时后不退出，导致日志只显示 `Windows speech exited with 2` 的问题。
- [x] 桌面模式下不再在主工作台右下角渲染 `.cat-dialog`，对话内容改由 Electron 小猫气泡窗口承载。
- [x] 启动时把旧的 `92x92` 小猫窗口设置规范化为 `360x190`，避免跟随小猫的消息气泡被裁切。
- [x] 桌面语音改为手动开关：点小猫开始持续听写，再点小猫写入停止信号并提交汇总文本。
- [x] 重新执行 `npm run desktop:pack`，并同步 `D:\YayaMindBuild\release\win-unpacked` 到真实入口 `D:\YayaMind`。
- [x] 从 `D:\YayaMind\YayaMind.exe` 启动验证，本地 API `/api/bootstrap` 返回 200。

## 2026-06-15 修复前端 404

- [x] 修复打包版桌面应用前端 404 问题
- [x] `electron/server-runner.cjs` 添加 `DESKTOP_STATIC_DIR` 环境变量
- [ ] 阶段收口时重新打包并同步到 `D:\YayaMind`

## 2026-06-15 日程陪伴感与个人习惯设置

- [x] 到点提醒只在刚触发时冒泡，旧提醒不在每次打开日程时重复提醒。
- [x] 没有提醒时按凌晨/早上/中午/下午/晚上/深夜显示亲切问候。
- [x] 将作息、休息制度和节假日开关放到个人画像页的个人习惯区。
- [x] 起床/睡觉时间会影响一周日程时间轴显示范围。
- [x] 休息日用浅暖色标记，不使用灰色。
- [x] 右侧日期详情支持小加号手动新增日程。
- [x] 手动新增日程保存后写入 `events.jsonl`，刷新后按时间顺序进入当天日程。
- [x] 桌面小猫悬浮窗支持上方消息气泡，规划/提醒文案跟着小猫走。
- [x] 项目待办字体和行距压小，提升信息密度。
- [x] 接入 2026 年中国法定节假日/调休数据，区分休息日和补班日；2026-06-19 至 2026-06-21 端午假期在周视图显示为休息日。
- [x] 已执行 `npm run desktop:pack` 并同步到 `D:\YayaMind`；从真实入口冷启动后 `/api/bootstrap` 返回 200。小猫气泡/语音跟随效果仍需用户在真实桌面交互中点小猫人工确认。

## 2026-06-16 MVP 收尾体验修复

- [x] 本轮补充：项目待办拖拽改成先长按选中，再拖动；待办项和项目卡片都会显示虚线选中框，避免误把多条待办一起移动。
- [x] 本轮补充：待办拖拽规则收窄为两类：同项目内按蓝色插入线上下排序，跨项目释放到项目框内后追加到目标项目末尾。
- [x] 本轮补充：项目卡片拖拽改为按蓝色插入线插到目标项目上方或下方，去掉含糊的释放预览。
- [x] 本轮补充：右侧今日详情分成“日程”和“待办”两个独立滚动区；有 `dueAt` 的项目待办进入下方待办区，不再要求必须是具体时间点任务。
- [x] 本轮补充：“定闹钟 / 起床 / 起来 / 睡前”这类语音输入归为生活待办，桌面语音 auto commit 时会写入 `tasks.jsonl`。
- [x] 本轮 source/dev 验证：`npm run build` 通过；临时 `YAYAMIND_DATA_DIR` 验证闹钟句式 commit 写入 `tasks.jsonl` 且项目为生活；临时数据验证 6/16 普通 deadline 待办进入当天 `calendar.tasks`；Browser 验证前端已加载长按选中、插入线和右侧分区样式。
- [x] 本轮正式入口同步：`npm run desktop:pack` 通过；已同步 `D:\YayaMindBuild\release\win-unpacked` 到 `D:\YayaMind`；从 `D:\YayaMind\YayaMind.exe` 冷启动后 `/api/bootstrap` 返回 200；真实入口 API 已返回 6/16 “流浪猫盒子”工作待办；Browser 刷新后右侧“日程 / 待办”分区可见。
- [x] 本轮补充：未归类/无命名分组也可通过删除按钮清空其中待办；项目卡片改为稳定两列等宽，不再被任务文本长短撑出不同宽度。
- [x] 本轮补充：项目卡片和待办拖拽增加释放位置预览；待办标题/备注编辑改为按内容自适应高度的多行输入。
- [x] 本轮补充：`生活新增一条待办` 这类“已有项目名 + 新增待办”语音句式会归入已有项目，不再新增错误项目或落到未归类。
- [x] 本轮补充：新一轮小猫听写开始时清空上一轮成功气泡；成功后的后续提示 5 秒后退场，实时转写/正在听优先展示。
- [x] 本轮开发态验证：`npm run build` 通过；Browser 验证待办页四个项目卡片同宽两列展示；临时数据目录验证 `生活新增一条待办` 写入项目为 `生活`。
- [x] 复盘补同步：已执行 `npm run desktop:pack`，同步 `D:\YayaMindBuild\release\win-unpacked` 到真实入口 `D:\YayaMind`，从 `D:\YayaMind\YayaMind.exe` 冷启动后 `/api/bootstrap` 返回 200；真实资源包含本轮拖拽预览和小猫气泡优先级修复。
- [x] 缩窄窗口时右侧新增日程加号不再消失；全局最小宽度从 1220px 降到 760px，窄视口验证无横向溢出。
- [x] 小猫气泡反馈 5 秒后自动隐藏，桌面 `electron/bubble.html` 和 Web message 清理保持一致。
- [x] 周视图日期不再叠加“今天”，休息日不再叠加“休/假”文字，改用马卡龙色块区分休息日 / 法定假期 / 补班日。
- [x] 周视图事件标题压缩为 2-3 个字，例如“要去健身房”显示为“健身”。
- [x] 个人画像页只保留一个“个人画像”标题，作息 / 休息 / 节假日开关对齐，保存习惯按钮仅在编辑后显示。
- [x] 右侧详情改为原卡片原地编辑；长文本字段使用多行输入，时间输入框缩短并支持点击输入框任意位置打开时间选择。
- [x] 项目待办删除分页，改为两列滚动展示；待办支持拖到别的项目或同项目内调整顺序，项目卡片支持拖拽排序；新增 `order` 字段持久化顺序。
- [x] 项目标题右侧新增圆形加号；空新增待办在失焦或点击外部后自动收起。
- [x] 开发态验证：`npm run build` 通过；Browser 验证右侧编辑只剩保存/删除、具体安排不再空、待办无分页、画像控件为暖色、6/19-6/21 法定休息日背景色生效。
- [x] 正式入口同步：`npm run desktop:pack` 通过；`D:\YayaMindBuild\release\win-unpacked` 已同步到 `D:\YayaMind`；真实入口 `D:\YayaMind\YayaMind.exe` 已启动，`/api/bootstrap` 返回 200。

## 2026-06-15 桌面版阶段收口发布规则

- [x] 明确只改源码目录：`D:\obsidian\MyVault\07_项目\个人助手`。
- [x] 明确安装目录 `D:\YayaMind` 是用户真实启动入口，不作为日常手动开发目录。
- [x] 复盘/阶段结束时再执行打包和同步，不在每个小功能修改后实时同步安装目录。
- [x] 阶段收口时必须把 `D:\YayaMindBuild\release\win-unpacked` 同步到 `D:\YayaMind`。
- [x] 同步后从 `D:\YayaMind\YayaMind.exe` 或桌面快捷方式启动验收。
- [x] 视觉资源收口时清理旧备份图、旧 hashed 图和临时预览图，避免旧小猫资源回流。
- [ ] 后续可以把打包、同步、清理旧资源、启动真实入口验收做成一个 `desktop:release-local` 脚本。

## 2026-06-12 语音交互协议与新用户教程

- [x] 桌面语音识别支持实时转写草稿进入输入区域。
- [x] 听写中再次单击小猫等于取消当前会话，并清空当前输入/追问状态。
- [x] 手动取消不再提示“没听清”，日志改为 `windows-speech-cancelled`。
- [x] `明天上午有会要开` 这类缺具体时间的日程输入会追问具体几点，不直接写入。
- [ ] 设计新用户教程：单击开始说话、再单击取消、双击开关工作台、追问时怎么补充、听不清时怎么重说。
- [ ] 后续评估语音唤醒：默认关闭，用户打开后只监听“呀呀/丫丫/Yaya”，唤醒后进入当前听写链路。

## 2026-06-12 桌面小猫第二轮与 D 盘迁移

- [x] 去掉灰色方块/头像按钮感，改成透明背景的完整小布偶猫睡觉态和倾听态。
- [x] 鼠标悬停和拖动分别使用 `grab` / `grabbing`。
- [x] 单击触发语音输入，并在小猫上显示“我在听”反馈。
- [x] 麦克风/语音不可用或启动失败时回传 error 状态并给明确提示。
- [x] 单击、双击、拖动彻底区分：单击延迟判断，双击只打开主工作台，拖动超过阈值才开始。
- [x] 小猫窗口 hide/show/close/crash 有日志，渲染进程异常退出后自动重建。
- [x] 托盘保留打开主工作台、显示/隐藏小猫、开机自启和退出入口。
- [x] `npm run desktop:pack` / `npm run desktop:dist` 输出到 `D:\YayaMind\release`。
- [x] 打包版启动后本地 API 自启动，`/api/bootstrap` 返回 200。
- [x] Electron 运行数据固定到 `D:\YayaMind\userData` 和 `D:\YayaMind\sessionData`。
- [x] npm、Electron、electron-builder 缓存迁移到 `D:\YayaMind\cache`。
- [x] 清理 C 盘 `AppData\Local\YayaMind`、`AppData\Roaming\YayaMind` 和临时测试包。
- [ ] 后续补正式 `.ico` 图标，替换默认 Electron 图标。
- [ ] 后续做真实人工压力验收：连续单击 10 次、双击 5 次、拖动几次，小猫不消失。


## 2026-06-12 桌面悬浮小猫 MVP

- [x] 选定第一版桌面壳路线：先用 Electron 复用现有 Web 工作台和本地 API，不重写业务逻辑。
- [x] 新增 Electron 主进程，支持创建透明置顶的桌面悬浮小猫窗口。
- [x] 移除悬浮小猫靠边吸附/半隐藏/鼠标移入展开逻辑，拖到哪里就停在哪里，并持久化位置。
- [x] 桌面悬浮小猫改用原布偶猫头像资源，保留圆形橙黄色边界，不再使用新绘制的猫脸。
- [x] Electron 模式下隐藏主工作台内的第二个小猫脸，只保留桌面悬浮小猫作为入口。
- [x] 桌面小猫单击通过 IPC 触发主工作台原有语音输入逻辑，双击只打开/聚焦主工作台。
- [x] 桌面小猫新增睡觉态/倾听态样式，去掉只靠 `Zz` 的状态表达。
- [x] 新增系统托盘入口：打开主工作台、显示/隐藏小猫、开机自启、退出应用。
- [x] 新增开机自启设置持久化，并通过 `app.setLoginItemSettings` 应用。
- [x] 新增 Windows 打包脚本和 `electron-builder` 基础配置。
- [x] 新增 `npm run desktop:dev`，同时启动本地 API、Vite 前端和 Electron 桌面壳。
- [x] 新增 `npm run desktop:open`，用于在本地服务已启动时只打开桌面悬浮球。
- [ ] 在真实桌面环境中做一轮手感验收：拖动停止位置、无灰底、无第二个猫脸、单击听写、双击打开主窗口、托盘菜单、关闭主窗口后小猫保留。
- [x] 补齐生产包内本地 API 自启动，确保 EXE 在未运行 `npm run dev:server`/`npm run dev:web` 时也能完整使用业务功能。
- [ ] 后续补正式 `.ico` 图标、安装包细节和全局快捷键。
- [ ] 手机端另起阶段评估：优先 PWA/Capacitor，等桌面入口稳定后再做。

## 2026-06-10 项目收尾与面试材料

- [x] 新增面向 AI 产品经理面试的 PRD 文档。
- [x] 新增面向面试讲解的 SDD 整合文档。
- [x] 新增竞品分析文档，覆盖 Motion、Sunsama、Reclaim、Todoist、Notion Calendar、TickTick、Akiflow。
- [x] 新增项目详情文档，讲清前后端结构、API、数据文件和核心数据流。
- [x] 新增面试高频 Q&A 文档。
- [x] 新增面试材料目录索引。
- [x] 更新 README、PROJECT_CONTEXT、`docs/archive/TASK_LOG.md` 和作品集入口，串联本轮文档。

## 2026-06-07 新对话交接 TODO

- [x] 接入或增强语音转写后的 AI 改写/纠错：目标是把明显误识别的词先修正，再进入项目待办/日程意图解析。
- [x] 继续统一全站中文字体和字号：整体再小一点，视觉更接近油画/铅笔涂鸦感，避免黑体感。
- [x] 右侧日程详情继续做字段级点击编辑：点文字原地编辑、失焦自动保存，删除保留但降低视觉权重。
- [ ] 做真实浏览器视觉验收：重点检查项目待办完成下沉动画、项目间拖拽、拖到月历日期、普通抓取拖拽光标、月历 deadline 色点。
- [ ] 继续处理分栏缩放体验：拖动中间分界线时，待办和日程文字/布局需要稳定适配宽度。
- [x] 梳理普通提醒、出门提醒和截止任务在右侧详情里的最终分区，避免“具体安排”之外的板块被误删。
- [ ] 继续压低交互延迟：语音纠错和提交目前已避免重复纠错，但仍可继续减少重复 AI 解析。

## 2026-06-04 冲突判断与周视图展示收敛

- [x] 日程冲突不再按标题相似判断；`开会` 这类归纳标题重复不代表日程重复。
- [x] 日程冲突只在真实时间范围重叠时触发，前后相邻的时间块不算重叠。
- [x] 冲突确认只给用户动作选项：`修改 / 重叠 / 取消`，不展示内部实现逻辑。
- [x] 中间周视图日程块只显示短标题，不显示 `15:00-16:00` 之类时间文字。
- [x] 中间周视图日程块改为透明玻璃描边，让背景小时数字和虚线从块后透出来。
- [x] 右侧详情删除“日期详情”标题、冲突解释、“目的”和“备注”冗余字段。
- [x] 右侧编辑表单改为标题、月/日/开始/结束、准备事项和操作按钮；准备事项默认一行。
- [x] 准备事项和备注去重；“带笔记本电脑”只写入准备，不再复制到备注。
- [x] 扫描并修复当前页面可见乱码；后续中文文案编辑必须使用 UTF-8 读写和乱码特征扫描。

## 2026-06-04 语音理解文案与日程确认结构

- [x] 修复页面中残留的乱码/错字文案，重点包括确认预览、右侧详情、编辑/取消/提醒等用户可见文字。
- [x] 输入理解预览改为 `标题 / 具体内容 / 提前准备` 三块，不再用零散字段堆叠。
- [x] 标题优先归纳为 `开会 / 上课 / 面试 / 提醒 / 任务`，不再简单截取原话前几个字。
- [x] DeepSeek 只作为旁边小字 `DeepSeek 参与` 展示，不写进主标题。
- [x] 具体内容支持分行展示时间、地点、内容和估时；地点可从“在软件园开会”这类表达中提取。
- [x] 准备事项支持 `提前准备 / 需要准备 / 要准备 / 带上` 等表达。
- [x] 语音输入能区分新增、修改和补充：补充备注/准备事项优先写到已有安排，修改语气优先修改已有安排。
- [x] 时间重叠时只给用户动作选择：`修改 / 重叠 / 取消`，不暴露移动/替换/待定等内部逻辑。

## 2026-06-04 语音写入确认与首屏修正

- [x] 左侧导航品牌改为横向两行 `Yaya / Mind`，不再竖排。
- [x] 小猫气泡首屏默认隐藏，不再显示历史删除/提示文案。
- [x] 新增写入后 `确认 / 修改 / 取消`，并在写入后自动跳到对应日期时间。
- [x] 支持对刚写入事项继续语音修改日期、时间、备注和准备事项。
- [x] 支持“给明天晚上的会加备注/准备事项”这类已有会议补充。
- [x] 今天列半透明，时间轴缩短为更像长方形的内部滚动区域。
- [x] 语音停顿自动写入保持 1 秒，减少等待感。

## 2026-06-04 本周视图与未来安排逻辑

- [x] 默认滚动让今天红线位于可视区上三分之一。
- [x] 未来安排按钮缩小并上移，贴近上边缘。
- [x] 中间面板删除 YayaMind logo/wordmark，品牌文字移动到左侧导航栏顶部。
- [x] 本周视图固定显示周一到周日 7 列，缩小时也不丢周五周六。
- [x] 过去日期弱化并压缩信息，当前/未来日期保留更高可读性。
- [x] 未来安排只显示本周之后的未来日期，不再显示本周剩余安排。
- [x] 后端返回本周以外的未来安排日期，修复“下周周四/下周周五”日期推断。

## 2026-06-04 中间日历和语音解析

- [x] 去掉中间顶部“一周安排”和 YayaMind 文字标题，改为轻量 logo/wordmark。
- [x] 左上角品牌图标改为“丫”字 YayaMind 标识方向。
- [x] 每天的时间轴改为内部滚动的 0:00-24:00 长轴，默认滚到 7 点附近。
- [x] 准点线加深，10 分钟刻度变轻，便于拖动到精确分钟。
- [x] 增加“未来安排”入口，可聚焦有安排的未来日期，并可回到今天。
- [x] 小猫语音停顿自动写入改为 1 秒。
- [x] 小猫增加“正在听”和“思考中”的视觉状态。
- [x] 修复“明天下午三点开始开会”仍被追问时间/活动的问题。

## 2026-06-04 时间轴与语音追问体验

- [x] 时间轴显示范围改为 7:00-24:00。
- [x] 时间轴增加 20 分钟细虚线，方便拖到 14:20 等具体时间。
- [x] 小猫点击和拖动拆开：点击开始说话，按住移动才拖动。
- [x] 小猫语音状态去掉冗余提示文案。
- [x] 追问补充信息后自动继续听，不需要再次点击小猫。
- [x] 不像安排/任务的识别内容先追问确认，不直接写入。
- [x] 追问/冲突选择卡片统一浅底深字，提升可读性。

## 2026-06-04 右侧滚动与待补充分类

- [x] 去掉日历块上下边缘的可见拖动提示框，仅保留悬停拖动光标。
- [x] 右侧日期详情独立滚动，左侧导航和中间一周安排固定不随页面滚动。
- [x] 含糊的“在这之前记得把那个……”类截止任务归入待补充事项，不再作为截止任务展示。

## 2026-06-04 本轮体验修正

- [x] 小猫对话框改为向上展开，不再把头像往下顶到点不到的位置。
- [x] 语音停顿自动写入从 4 秒改为 2 秒。
- [x] 待补充事项不再显示在中间一周安排列顶部，只在右侧待补充事项中展示。
- [x] 日历块拖动增加实时预览；上下边缘调整开始/结束时间的手柄增强可见性。
- [x] 会议/开会卡片颜色改为暖色系。
- [x] 出门提醒移除随机感强的全日泛提醒，并收紧外出识别关键词。

## 2026-06-03 本轮反馈修正

- [x] 日期详情编辑改为右侧卡片内联编辑，不再使用事项详情悬浮框。
- [x] 一周安排和日期详情之间增加可拖动宽度调节。
- [x] 待定事项命名和展示改为“待补充事项”，不再混入时间安排。
- [x] 增加“截止任务”展示，并在一周安排时间轴上显示截止线标记。
- [x] 右侧提醒合并为“提醒”，下分出门提醒和事件提醒，并保留书面提醒入口。
- [x] 移除右侧执行记录展示。
- [x] 小猫语音入口改为点击小猫后自动听写，停顿 4 秒自动写入。
- [x] 时间不明确/需要补充的提示增强醒目度。
- [x] 天气提醒默认不写死上海；城市名仅在用户配置后显示。

## 历史阶段记录：旧 Web / V1.1 口径，不作为当前版本主口径

目标：Web 模拟版个人助手的产品方案、MVP 拆分、SDD 主体和第一轮 MVP 代码实现已完成；当前进入演示包装和后续增强阶段。

## 历史 V1.1 阶段任务

目标：MVP 已完成，V1.1 优先提升基础好用程度。首页第一屏继续保持“一周安排”，右侧展示选中日期详情；AI 感优先体现在输入理解，小猫保持陪伴型助手角色。

### V1.1 P0：输入理解确认

- [x] 增强输入解析结构：从自然语言中提取事项类型、标题、时间、具体事项、准备事项、预估时长和提醒。
- [x] 增加写入前结构化确认预览，让用户能确认或修正小猫理解的结果。
- [x] 支持会议类输入：会议时间、具体事项、准备事项和关联提醒。
- [x] 支持任务类输入：截止时间、预估时长、准备事项和待安排状态。
- [x] 对时间不明确、类型不确定、任务疑似重复和真实时间冲突事项只追问一个关键问题。

### V1.1 P1：日期详情增强

- [x] 右侧日期详情展示选中日期的完整信息，而不是只展示短标题。
- [x] 展示具体事项、准备事项、提醒和执行记录，避免目的/备注重复堆叠。
- [x] 支持点击日期或事项后，右侧定位到对应日期/事项详情。
- [x] 今日详情继续展示未处理提醒和执行记录。
- [x] 保持一周安排高信息密度：中间区域只展示短标题和必要状态。

### V1.1 P2：轻量动态安排与陪伴奖励反馈

- [x] 对新增事项进行轻量冲突、任务重复和过载判断；日程不按标题相似判重复。
- [x] 时间不足或计划冲突时只给 1-2 个调整方案。
- [x] 完成任务、完成提醒、记录进度后，小猫给短而温和的陪伴式反馈。
- [x] 做不完或延期时，引导记录原因，不使用责备式语气。
- [x] 将反馈和执行记录沉淀到日期详情中，方便当天回看。

## 优先级 P0

- [x] 确认技术栈：前端、后端、本地文件读写、语音识别、AI 模型接入。
- [x] 设计 Obsidian 数据文件 schema：events、tasks、work_logs、reviews、goals、profiles、settings。
- [x] 拆分 SDD 文档：产品范围、数据存储、输入解析、页面状态、日历视图、今日面板、冲突重排、小猫浮窗、提醒、AI 策略。
- [x] 设计 Web 面板第一版原型：左侧导航、中间 7-10 天日历列视图、右侧今日执行面板。
- [x] 设计小猫浮窗第一版交互：点击、拖动、气泡、文本输入、语音输入确认流程。
- [x] 设计本地 API 契约：初始化数据、日历、今日面板、输入解析、工作状态、提醒、冲突重排。
- [x] 制定第一轮实施路线：脚手架、数据层、本地 API、Web 面板骨架、输入写入闭环。

## 优先级 P1

- 当前代码初版进展：已创建 React/Vite 前端、Fastify 本地后端、JSON/JSONL 数据层、三栏页面骨架、深色版 `YayaMind` UI、`Y + 猫爪` 融合图标、布偶猫风格输入入口、工作状态接口、提醒状态接口、规则版输入解析、解析预览、自然周一周安排、点击日期查看右侧详情、日历时间轴、进度快捷记录、写入前冲突拦截、轻量冲突处理方案、任务重复/过载检测、浏览器内提醒触发、提醒稍后/忽略处理、提醒处理历史、重叠时间块并列展示和可拖动布偶猫浮窗；已安装依赖、启动本地服务并通过构建验证。
- [x] 实现基础数据读写：从 Obsidian 数据目录读取 JSON/JSONL 并写入新记录。
- [x] 实现自然语言输入分类：日程、任务、时间块、工作状态、进度、复盘、生活提醒。
- [x] 实现日历列视图：过去时间置灰、当前时间线、类型颜色区分。
- [x] 实现日期详情面板：点击某一天后展示具体安排；选中今天时展示提醒处理和执行记录。
- [x] 实现冲突检测：时间重叠、任务冲突、疑似重复、计划过载、时间不明确。
- [x] 实现轻量动态重排：只给 1-2 个方案，由用户确认。
- [x] 继续打磨 YayaMind UI：自然周一周安排、日期详情、布偶猫浮窗高级感、左上角 `Y + 猫爪` 图标。

## 优先级 P2

- [x] 实现阶段性目标：目标、状态更新、进展入口。
- [x] 实现个人画像雏形：时间习惯、任务估时、生活节奏、工作偏好。
- [x] 实现浏览器内提醒：网页打开时小猫冒泡提醒，支持浏览器通知。
- [x] 生成每日/每周 Markdown 总结到 Obsidian。
- [x] 准备作品集包装材料：README、架构图、演示数据、截图、功能说明。

## 暂缓

- [ ] 真正桌面应用。
- [ ] 系统级常驻提醒。
- [ ] 自动看屏幕或截图分析。
- [ ] 手机 App。
- [ ] 微信入口或小程序。
- [ ] 多用户账号系统。
- [ ] 睡眠/状态记录。

## V1.1 试用反馈继续打磨

- [x] DeepSeek API Key 接入本地后端，提供 `/api/ai/status` 用于确认是否正在使用 AI。
- [x] 模糊时间追问使用独立补充回答卡片；用户补充“下午2:00~3:00”这类时间段后应合并原始输入重新解析。
- [x] 重复、冲突、过载等需要用户决策的问题使用独立选择卡片，不再塞回旧的理解预览。
- [x] 右侧日期详情中的日程、待定事项和任务支持基础编辑与删除；删除采用软删除/取消。
- [x] 一周安排支持当天列顶部的待定事项/任务短预览，避免无具体时间的事项跨天漂在时间轴里。
- [x] 一周安排时间轴覆盖 0:00-24:00，兼容凌晨安排和较晚作息。
- [x] 将右侧详情编辑从浏览器 prompt 升级为内联编辑表单；当前表单收敛为标题、月/日、开始/结束时间、准备事项等必要字段。
- [x] 支持日历时间块拖拽移动和上下拖拽调整时长，按 10 分钟粒度吸附。
- [x] 给时间冲突块增加轻量视觉提示；冲突只作为小标签/并列状态展示，不在右侧详情重复解释或引导用户必须改时间。
- [x] 进一步强化语音输入入口，让“小猫对话”优先于普通文本框：移除底部普通输入框，改为语音实时转写和确认。
- [x] 执行记录支持点击后编辑和删除；提醒生成的历史行保持只读。
- [x] 修复取消/移动后的旧日程仍参与冲突检测的问题，避免明确时间仍被误追问。
- [ ] 增加用户作息偏好设置入口，例如常用起床/睡觉时间和默认日历显示范围。
- [x] 增加天气/出门提醒：识别外出安排，结合天气信号在出门前提醒带伞等准备。
- [x] 右侧普通提醒区移除，仅保留日期具体安排、出门天气提醒和执行记录，减少详情面板噪音。
- [x] 详情内联编辑保留准备事项字段，准备事项支持换行/逗号/顿号拆分保存；移除目的、备注等容易重复的字段。
- [x] 将整体 UI 初步切换为浅色、橘黄/肉色调、涂鸦可爱风，并强化过去/未来时间区分。
- [ ] 继续精修涂鸦风细节：手写感分隔线、安排块质感、可爱但不幼稚的图标和空状态。
